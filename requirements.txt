# =====================================================================
# Radar Combustivel - Python dependencies
# Tested with Python 3.10+
# =====================================================================

# MongoDB driver (Change Streams support requires PyMongo 4.x + replica set)
pymongo==4.8.0

# Redis client with RediSearch / RedisTimeSeries / RedisJSON modules
redis==5.0.7

# Streamlit dashboard
streamlit==1.36.0
streamlit-autorefresh==1.0.1

# Data manipulation / charts
pandas==2.2.2
numpy==1.26.4
plotly==5.22.0
pydeck==0.9.1

# Geo helpers
shapely==2.0.4

# Env / utilities
python-dotenv==1.0.1
tenacity==8.5.0
rich==13.7.1

# PDF / DOCX deliverable generation
reportlab==4.2.2
svglib==1.5.1
python-docx==1.1.2
Pillow==10.4.0
